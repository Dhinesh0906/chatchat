version: "3.9"

services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    command: ["ollama", "serve"]

  api:
    build: ./api
    container_name: api
    depends_on:
      - ollama
    ports:
      - "5000:5000"
